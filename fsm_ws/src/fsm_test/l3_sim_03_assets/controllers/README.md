# ros2_control Mock Hardware Config

Put ros2_control mock hardware config here.

Required before L3-SIM-03:

- controller manager config
- joint_state_broadcaster
- arm trajectory controller
- torso trajectory controller, if used by the real robot
- controller names matching MoveIt controller mapping
