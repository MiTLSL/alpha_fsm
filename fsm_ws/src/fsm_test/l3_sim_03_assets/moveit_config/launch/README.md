# MoveIt Launch Fragments

Put official MoveIt launch files or adapter launch fragments here if L3-SIM-03
cannot directly reuse an upstream MoveIt config package.

Preferred long-term option: keep the official MoveIt config as its own ROS
package and reference it from the L3-SIM launch.
