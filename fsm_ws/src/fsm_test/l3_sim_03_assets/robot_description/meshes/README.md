# Meshes

Place mesh files referenced by the robot URDF/xacro here, or keep this
directory as a symlink target to the official robot description package.

Supported formats are whatever the upstream URDF references, usually:

```text
*.dae
*.stl
*.obj
```
