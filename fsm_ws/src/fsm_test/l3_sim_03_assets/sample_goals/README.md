# Sample Grasp Goals

Put standard and boundary `ExecutePairGrasp` goal fixtures here.

These fixtures should cover:

- one standard reachable pair
- one IK boundary pair
- one trajectory/collision boundary pair

The smoke script for L3-SIM-03 will use these to assert success and error-code
mapping.
