# 05 配置与参数清单

> 本文档列出 5 个生产 YAML 文件 + 1 个 M2-SIM / L3-SIM 测试专用 `sim.yaml` 的全部参数，含默认值、单位、可调范围、生效节点。**现场调参主要动 `business.yaml`，其他生产文件开发期定型后较少改动；`sim.yaml` 和 L3-SIM-03 物理仿真资产不进入真机 bringup**。

---

## 0. 加载流程

```text
launch 文件加载 YAML
  → 注入到对应节点的 ROS2 parameters
  → 节点 __init__ 时 declare_parameter() + get_parameter()
  → 缓存到 ctx.config（@dataclass(frozen=True)）
  → 状态代码读 ctx.config.xxx，禁止直接调 get_parameter()
```

支持运行时 reload：`ros2 service call /<node>/reload_config std_srvs/Trigger`，由 node.py 实现，重新读 YAML 并替换 ctx.config。

生产 bringup 默认加载 `business.yaml` / `fsm.yaml` / `interfaces.yaml` / `error_codes.yaml` / `logging.yaml`。`sim.yaml` 只由 `bringup_with_kinematic_sim.launch.py`、L3-SIM-03 轻量物理仿真 launch 加载给 `fsm_test/sim` 节点和仿真 profile，生产节点不得依赖其中字段。

---

## 1. params/business.yaml（现场频繁调）

> 业务硬参数。所有需要现场标定/调试的数字都在这里。

```yaml
# params/business.yaml
business:
  # —— 货箱与网格 ——
  box_size:                                  # 标准纸箱尺寸（米）
    length: 0.40                             # x，深度
    width:  0.40                             # y，宽度
    height: 0.40                             # z，高度

  grid_shape:
    rows: 5                                  # 一面墙的行数
    cols: 5                                  # 一面墙的列数

  left_phase_cols:  [0, 1, 2]                # 左作业位位于第 2 个箱子前方，可覆盖正对列和附近两列
  right_phase_cols: [2, 3, 4]                # 右作业位位于第 4 个箱子前方，可覆盖正对列和附近两列
  max_adjacent_column_height_delta: 1        # 抓后任意相邻列剩余高度差上限，防止箱墙倾覆

  # —— 网格匹配容差（PhasePerceptionFSM / WallMappingFSM 用） ——
  match_tolerance:
    y: 0.15                                  # m，横向（左右）容差，TODO 现场验证
    z: 0.15                                  # m，纵向（上下）容差，TODO 现场验证
    # x 方向（深度）不参与匹配，由墙面平面决定

  # —— 任务完成判定 ——
  empty_confirm_frames: 5                    # 多帧无箱才确认完成，TODO 视觉准确度验证后再调
  empty_confirm_window_sec: 3.0              # 多帧采集时间窗

  # —— 重试 ——
  max_retry_per_slot: 3                      # 单 slot 抓取失败重试上限
  allow_single_arm_grasp: true               # 双抓不满足高度安全或当前 phase 只剩 1 个顶部箱时，是否允许单臂抓

  # —— 底盘作业位姿（map 系下的完整 pose）——
  # TODO: 全部需要现场实测标定。yaw 单位 rad。
  observation_pose:
    x: 0.0                                   # TODO
    y: 0.0                                   # TODO
    z: 0.0
    yaw: 0.0                                 # TODO（朝向墙）

  left_phase_workpose:
    x: 0.0                                   # TODO
    y: 0.30                                  # TODO（向左偏 30cm 示例）
    z: 0.0
    yaw: 0.0                                 # TODO

  right_phase_workpose:
    x: 0.0                                   # TODO
    y: -0.30                                 # TODO（向右偏 30cm 示例）
    z: 0.0
    yaw: 0.0                                 # TODO

  # 作业位到墙面的目标距离与朝向（FineAlignment 用）
  desired_distance_to_wall: 0.60             # m，TODO 现场实测
  desired_yaw_to_wall:      0.0              # rad
  fine_alignment_tolerance:
    position: 0.02                           # m，对齐后允许的最大平移误差
    yaw:      0.02                           # rad

  # —— 固定放置位（base_link 系）——
  # TODO: 全部需要现场实测。
  fixed_place_pose:
    x: 0.50                                  # TODO
    y: 0.0                                   # TODO
    z: 0.80                                  # TODO（输送线入口高度）
    qx: 0.0
    qy: 0.0
    qz: 0.0
    qw: 1.0

  # —— 机械臂工作空间（PairSelection 可达性判断用，AABB 简化）——
  # 都是 base_link 系
  left_arm_workspace:
    x_min:  0.30
    x_max:  0.90
    y_min: -0.10                             # 左臂不能到极右
    y_max:  0.60
    z_min:  0.30
    z_max:  1.80

  right_arm_workspace:
    x_min:  0.30
    x_max:  0.90
    y_min: -0.60
    y_max:  0.10                             # 右臂不能到极左
    z_min:  0.30
    z_max:  1.80

  # —— 视觉感知开窗 ——
  mapping_window:                            # WallMappingFSM 用
    frames: 10                               # 全局开窗收 10 帧
    timeout_sec: 3.0
    min_valid_detections: 8                  # 滤波后至少这些个才能继续

  local_window:                              # PhasePerceptionFSM 用
    frames: 5                                # 局部开窗收 5 帧
    timeout_sec: 1.5

  detection_filter:
    confidence_min: 0.5
    size_tolerance: 0.10                     # m，size 偏离 box_size 超过这个就丢
    class_label: "box"

  # —— perception_adapter_node 健康阈值 ——
  perception_adapter:
    publish_rate_hz: 10.0                     # /perception/box_detections 输出频率上限
    camera_timeout_ms: 500
    lidar_timeout_ms: 500
    result_timeout_ms: 1000
    tf_timeout_ms: 200
    min_face_inlier_count: 20                 # face inlier 过低时 pose_valid=false

  # —— FINE_ALIGN 对墙微调 ——
  fine_alignment:
    timeout_sec: 15.0
    retry_limit: 1
    pass_frames: 5
    dist_tolerance: 0.02                      # m
    yaw_tolerance: 0.05                       # rad
    max_linear_x: 0.05                        # m/s，只允许小速度前后微调
    max_angular_z: 0.20                       # rad/s
    min_linear_x: 0.0                         # m/s，克服静摩擦用；默认 0 表示不强制最小速度
    min_angular_z: 0.0                        # rad/s
    linear_gain: 0.80
    angular_gain: 1.50
    min_detection_confidence: 0.50
    feedback_timeout_ms: 1000
    stop_cmd_repeats: 2                       # 退出 / cancel / estop 时连续发几帧零 /cmd_vel_align

  # —— navigation_manager_node 真实后端 ——
  navigation_manager:
    backend_mode: "fake_real"                 # fake_real / real；两者都走 adapter 代码，差别由 launch 连接 fake 还是真 Nav2
    nav2_client_wait_sec: 2.0
    service_timeout_sec: 2.0
    require_lifecycle_active: true
    require_amcl_pose: true
    require_chassis_ready: false              # 仿真默认关闭；真机联调打开后检查 /chassis_node/status
    amcl_timeout_sec: 1.5
    amcl_max_covariance: 0.25
    chassis_status_timeout_sec: 1.0
    estop_release_repeats: 3                  # clear_error 第 1 阶段连续发布 /estop=false 的次数

  # —— Isaac Sim 全流程仿真桥（sim.yaml）——
  sim:
    isaac:
      enabled: false
      use_sim_time: true
      perception:
        source_mode: "param_truth"            # param_truth | isaac_sdk；当前无 Isaac 环境先用参数/YAML 真值
        publish_rate_hz: 10.0
        scene_truth_file: ""                  # YAML: boxes: [{id, frame_id, center, size, yaw, confidence}]
        boxes_json: ""                        # 小规模调试可直接传 JSON 数组
        synthetic_latency_ms: 0.0
        view:
          max_distance_m: 3.0                 # 只发布视野内箱体，避免策略层看穿全场
          horizontal_fov_rad: 2.2
          z_min: -0.2
          z_max: 2.8
      chassis:
        initial_enabled: true
        initial_fault: false
        status_publish_rate_hz: 10.0

  # —— 真空 / 吸盘 ——
  vacuum:
    attach_threshold_kpa: -50.0               # 真空表读数越负吸附越强
    hold_ms: 150
    buildup_timeout_ms: 800
    sensor_timeout_ms: 200
    pressure_publish_rate_hz: 20.0
    mock_buildup_time_ms: 150
    mock_release_time_ms: 100
    hold_on_estop: true                       # 急停默认保持吸盘，避免未知姿态掉箱
    release_on_cancel: false                  # cancel 默认不自动放气，由上层确认安全姿态

  # —— 双臂抓取执行 / MoveIt 后端 ——
  pair_grasp_execution:
    backend_mode: "dry_run"                   # dry_run | fake_real | real
    fake_failure_mode: "NONE"                 # fake_real 注入：IK_FAIL / TRAJ_FAIL / COLLISION / MOVE_FAIL / VACUUM_NOT_REACHED
    stage_delay_sec: 0.03                     # s，状态反馈节拍，主要用于 dry_run / fake_real
    moveit_action_wait_sec: 2.0               # s，等待 MoveIt action / planning scene service 的基础超时
    moveit_planning_group: "dual_v5_arm_with_base"
    left_tip_link: "left_v5_tool0"
    right_tip_link: "right_v5_tool0"
    moveit_allowed_planning_time_sec: 5.0
    moveit_num_planning_attempts: 10
    moveit_velocity_scaling: 0.30
    moveit_acceleration_scaling: 0.20
    moveit_position_tolerance: 0.015          # m
    moveit_orientation_tolerance: 0.08        # rad
    input_pose_represents_box_center: true    # true 时由箱中心沿局部 X 推到接触面
    contact_standoff_x: 0.0                   # m，接触位额外退让量
    pregrasp_offset_x: 0.10                   # m，预抓位相对接触位的局部 X 偏移
    extract_offset_x: 0.10                    # m，抽离位相对接触位的局部 X 偏移
    retreat_offset_x: 0.10                    # m，放置后退让距离
    place_y_separation: 0.40                  # m，双臂放置目标在 Y 方向分开量
    attach_box_to_planning_scene: true        # 抓取后把箱体挂到末端，参与后续避碰
    attached_box_weight_kg: 2.0
    attached_box_touch_links: []              # 现场可追加吸盘/法兰 link；代码会自动补末端相邻 link
    self_collision:
      check_current_state: true               # 规划前调用 MoveIt /check_state_validity 检查当前整机状态
      require_dual_arm_group: true            # 强制使用覆盖左右臂和升降轴的 planning group
      required_group_name: "dual_v5_arm_with_base"
      joint_state_max_age_sec: 1.0            # s，/joint_states 超过该时间视为不可用于自碰检查
      state_validity_timeout_sec: 1.0         # s，等待 /check_state_validity 的超时
    collision_scene:
      manage_world_objects: true              # real 后端规划前是否管理临时 world collision objects
      remove_target_box_objects: false        # 仅当外部 scene 中已按 box_<slot_id> 命名目标箱时打开
      enable_container_obstacle: false        # 联调标定前默认关闭，避免未标定容器板挡住规划
      enable_static_box_wall_obstacles: false # 联调标定前默认关闭；打开后为目标箱周围箱墙生成开口障碍
      static_box_obstacle_inset: 0.002        # m，目标箱开口边缘收缩，避免开口与目标箱重叠
      static_box_wall_margin: 0.0             # m，箱墙障碍外扩余量
      container:
        center_x: 0.0                         # base_link 系，TODO 现场标定
        center_y: 0.0
        width: 2.2                            # m，容器内宽
        height: 2.4                           # m，容器内高
        length: 8.0                           # m，沿 X 方向长度
        wall_thickness: 0.03                  # m
        floor_z: 0.0                          # m，容器底面高度
    planning_workspace:
      x_min: -1.0
      x_max: 1.5
      y_min: -1.0
      y_max: 1.0
      z_min: 0.0
      z_max: 2.0
    skip_vacuum_check: true                   # M2 默认跳过真实压力闭环；真机吸附验证时关闭

  # —— 任务上限 ——
  max_walls: 0                               # 0=不限制；正数=最多抓 N 面墙
  max_task_duration_sec: 1800

  # —— 双臂冲突检查 ——
  dual_arm_min_y_separation: 0.05            # m，两 box 在 Y 方向最小间距
  dual_arm_min_xz_aabb_clear: 0.05           # m，X-Z 平面 AABB 最小间隙

  # —— 单臂分配模糊判定 ——
  arm_assign_ambiguous_y_diff: 0.05          # m，Y 差小于此值则记 WARN
```

**调参指引**：
- 现场新部署，先标定 `observation_pose / left_phase_workpose / right_phase_workpose / fixed_place_pose`，其他保留默认
- 视觉漏检多 → 调小 `match_tolerance` 或加 `mapping_window.frames`
- 视觉误检多 → 调高 `detection_filter.confidence_min`
- 抓取失败多 → 调大 `max_retry_per_slot`，或检查 workspace 设定是否过宽
- MoveIt 真后端联调 → 先切 `pair_grasp_execution.backend_mode=real`，再按标定质量逐步打开 `collision_scene.enable_container_obstacle` / `enable_static_box_wall_obstacles`
- 外部 planning scene 已加载箱体模型 → 保持 object id 与 `box_<slot_id>` 一致后，再打开 `collision_scene.remove_target_box_objects`
- 现场箱墙倾覆风险高 → 保持 `max_adjacent_column_height_delta=1`，不要为了减少底盘移动放宽该硬约束
- 任务过早完成（其实还有箱）→ 加大 `empty_confirm_frames`

---

## 2. params/fsm.yaml（开发期定型）

> FSM 框架参数：tick rate、每个状态超时、recovery 上限。开发期定，现场少改。

```yaml
# params/fsm.yaml
fsm:
  # —— 引擎参数 ——
  tick_rate_hz: 20                           # 主 tick 频率，急停响应最慢 50ms
  state_broadcast_rate_hz: 1                 # 状态心跳广播频率（除变化时）

  # —— 节点级超时 ——
  node_init_timeout_sec: 30
  node_shutdown_timeout_sec: 5

  # —— 状态超时表 ——
  # key 格式："<FSM_name>.<STATE_NAME>"，单位秒
  # 这里只列覆盖默认 10s 的状态；未列出的用 BaseState.timeout_sec=10
  state_timeouts:
    # RobotSystemFSM
    RobotSystemFSM.BOOTING: 30
    RobotSystemFSM.SELF_CHECK: 20
    RobotSystemFSM.PAUSED: 300

    # TaskFSM
    TaskFSM.RUN_TASK: 1800

    # WallDestackingFSM
    WallDestackingFSM.NAVIGATE_TO_OBSERVATION_POSE: 60
    WallDestackingFSM.NAVIGATE_TO_PHASE_WORKPOSE: 60
    WallDestackingFSM.RUN_WALL_MAPPING: 30
    WallDestackingFSM.RUN_PHASE_PERCEPTION: 20
    WallDestackingFSM.WAIT_PAIR_GRASP_RESULT: 120
    WallDestackingFSM.VERIFY_TASK_COMPLETE: 30
    WallDestackingFSM.WALL_ERROR_HANDLE: 30

    # WallMappingFSM
    WallMappingFSM.COLLECT_FRAMES: 3.5         # 同 business.mapping_window.timeout + 0.5 余量

    # PhasePerceptionFSM
    PhasePerceptionFSM.COLLECT_LOCAL_FRAMES: 2.0

    # WallRecoveryFSM
    WallRecoveryFSM.WAIT_MANUAL_RECOVERY: 600

  # —— Recovery 重试上限 ——
  recovery_max_attempts:
    RETRY_CURRENT_STATE: 3
    REPLAN: 2
    REPERCEPTION: 3
    SWITCH_TARGET: 5
    MOVE_BASE: 2
    RETREAT_SAFE: 1
    RELOCALIZE: 2
    REBUILD_GRID: 2
    WAIT_MANUAL_RECOVERY: 1
    ABORT_TASK: 1
    E_STOP: 1

  # —— Action 调用超时（client 侧）——
  action_send_goal_timeout_sec: 2
  action_get_result_timeout_sec: 120          # 上限，具体由 state_timeouts 控制
```

---

## 3. params/interfaces.yaml（一次定型）

> 各节点订阅/发布/调用的 Topic/Service/Action 名称、QoS、frame_id 命名。一次定型几乎不改。

```yaml
# params/interfaces.yaml
interfaces:
  # —— Topic 名 ——
  topics:
    # FSM 广播
    fsm_system_state:           "/fsm/system_state"
    fsm_task_state:             "/fsm/task_state"
    fsm_wall_destacking_state:  "/fsm/wall_destacking_state"
    fsm_active_substate:        "/fsm/active_substate"
    fsm_grid_snapshot:          "/fsm/grid_snapshot"
    fsm_grasp_pair:             "/fsm/grasp_pair"
    fsm_error:                  "/fsm/error"
    fsm_log_event:              "/fsm/log_event"

    # 视觉
    perception_detections:      "/perception/box_detections"
    perception_health:          "/perception/health"
    box_perception_result:      "/box_perception/result"
    camera_image_raw:           "/camera/camera/color/image_raw"
    camera_info:                "/camera/camera_info"
    cloud_registered_body:      "/cloud_registered_body"

    # 安全
    safety_status:              "/safety/status"
    safety_estop:               "/safety/estop"
    estop_button:               "/estop_button"

    # IO
    vacuum_cmd:                 "/vacuum/cmd"
    vacuum_pressure_raw:        "/vacuum/pressure_raw"
    vacuum_pressure:            "/vacuum/pressure"
    vacuum_health:              "/vacuum/health"
    io_state:                   "/io/state"
    joint_states:               "/joint_states"

    # 底盘 / 导航 adapter
    cmd_vel_align:              "/cmd_vel_align"
    estop:                      "/estop"

    # TF
    tf:                         "/tf"
    tf_static:                  "/tf_static"

  # —— Service 名 ——
  services:
    task_start:    "/task/start"
    task_pause:    "/task/pause"
    task_resume:   "/task/resume"
    task_cancel:   "/task/cancel"
    clear_error:   "/clear_error"
    safety_sw_estop: "/safety/sw_estop"
    nav_base_recovery: "/nav/base_recovery"
    moveit_apply_planning_scene: "/apply_planning_scene"
    moveit_check_state_validity: "/check_state_validity"
    reload_config: "/reload_config"           # 每个节点自带

  # —— Action 名 ——
  actions:
    run_wall_destacking: "/run_wall_destacking"
    navigate_to_pose:    "/navigate_to_pose"
    execute_pair_grasp:  "/execute_pair_grasp"
    moveit_move_group:   "/move_action"

  # —— Frame ID ——
  frames:
    map:           "map"
    odom:          "odom"
    base_link:     "base_link"
    body:          "body"                       # 上游 Fast-LIO 真实 body frame，仅 adapter 使用
    camera_init:   "camera_init"                # 上游 Fast-LIO 世界系，仅 adapter 使用
    d2_base_link:  "2d_base_link"               # Nav2 对外逻辑 2D base
    d2_body:       "2d_body"                    # Nav2 实际 robot_base_frame，仅 navigation_manager 使用
    camera_link:   "camera_link"
    left_tcp:      "left_tcp"
    right_tcp:     "right_tcp"
    wall_prefix:   "wall_"                    # 实际为 wall_0_frame, wall_1_frame, ...
    container:     "container_frame"

  # —— QoS 配置 ——
  qos:
    # latched-like：晚启动也能收到最新
    fsm_state:
      reliability: "RELIABLE"
      durability:  "TRANSIENT_LOCAL"
      history:     "KEEP_LAST"
      depth: 1

    fsm_log:
      reliability: "BEST_EFFORT"
      durability:  "VOLATILE"
      history:     "KEEP_LAST"
      depth: 200

    perception_detections:
      reliability: "BEST_EFFORT"
      durability:  "VOLATILE"
      history:     "KEEP_LAST"
      depth: 5

    perception_health:
      reliability: "RELIABLE"
      durability:  "TRANSIENT_LOCAL"
      history:     "KEEP_LAST"
      depth: 1

    safety_status:
      reliability: "RELIABLE"
      durability:  "TRANSIENT_LOCAL"
      history:     "KEEP_LAST"
      depth: 1

    safety_estop:
      reliability: "RELIABLE"
      durability:  "TRANSIENT_LOCAL"
      history:     "KEEP_LAST"
      depth: 1
```

---

## 4. params/error_codes.yaml（累积式增加）

> error 默认 recovery 与等级的现场覆盖。`overrides` 段优先于 ErrorCode enum 默认。

```yaml
# params/error_codes.yaml
error_codes:
  # —— 覆盖默认 level / recovery ——
  # 例如：现场实测发现真空漏气重试基本无效，直接换目标
  # 5102:
  #   recovery: "SWITCH_TARGET"
  #   max_attempts: 1
  overrides: {}

  # —— 现场静音某些 WARN（保留日志但不进 recovery 流程）——
  # 例如：误检过多但不影响业务
  # silenced:
  #   - 3221
  silenced: []

  # —— Recovery 全局上限（覆盖 fsm.yaml 中的同名设置）——
  # 留空表示用 fsm.yaml 的默认
  recovery_max_attempts: {}

  # —— FATAL 升级阈值 ——
  # 当某 error_code 在同一 task 内累计触发次数超过这个值，自动升级 FATAL
  escalate_to_fatal:
    5102: 5                                  # vacuum leakage 5 次就 fatal
    4022: 3                                  # navigation stuck 3 次就 fatal
```

---

## 5. params/logging.yaml（几乎不改）

```yaml
# params/logging.yaml
logging:
  # —— 输出路径 ——
  root_dir: "/var/log/blue_sword_fsm"        # 现场可改
  filename_pattern: "{node_name}_{date}.jsonl"
  date_format: "%Y%m%d"

  # —— 滚动策略 ——
  rotation:
    max_size_mb: 100                         # 单文件最大
    backup_count: 10
    rotate_at_midnight: true

  # —— 写入策略 ——
  buffering:
    flush_interval_sec: 1                    # 至多 1s 落盘
    queue_size: 10000                        # 异步队列上限

  # —— 必填字段（与 01 §6.2 一致）——
  required_fields:
    - ts
    - node
    - fsm
    - task_id
    - event
    - state

  # —— 事件级别 ——
  # 列出的事件类型会被记录；未列出的丢弃
  enabled_events:
    - state_enter
    - state_exit
    - transition
    - error
    - metric
    - task_summary

  # —— Context 快照 ——
  # 每个事件附带的 ctx 字段（防止整 ctx 序列化太大）
  ctx_snapshot_fields:
    - task_id
    - wall_index
    - phase: "current_phase"                 # 重命名输出键
    - state: "current_state"
    - error_code: "last_error.error_code"

  # —— Topic 同时镜像（可选，便于 UI 实时看）——
  mirror_to_topic: "/fsm/log_event"
  mirror_qos: "fsm_log"                      # 引用 interfaces.yaml 的 qos
```

---

## 5.1 params/sim.yaml（M2-SIM / L3-SIM 测试专用）

> 本文件只服务 `bringup_with_kinematic_sim.launch.py` 和 L3-SIM-03 轻量物理仿真 launch。它描述仿真世界、真值传感器、fake base 和仿真时间，不属于现场标定文件，也不得被 `bringup_full.launch.py` 加载。Gazebo world / model / physics 资源放在 `fsm_test/l3_sim_03_assets/`，不要塞进生产 YAML。

```yaml
# params/sim.yaml
sim:
  enabled: false                              # 只在仿真 launch 中置 true
  use_sim_time: true
  time_scale: 1.0                             # 长跑可设 10.0；生产节点不得据此改变业务逻辑

  scene:
    wall_count: 2
    active_wall_index: 0
    grid_origin_map:                          # 第一面墙左上角箱中心，map 系
      x: 1.20
      y: 0.80
      z: 1.80
      yaw: 3.14159
    wall_spacing_y: 2.50                      # 多墙沿 map.y 的间隔
    box_size:                                 # 默认跟 business.box_size 保持一致
      length: 0.40
      width: 0.40
      height: 0.40
    missing_slots: []                         # 例如 ["0,4", "3,2"]，用于缺箱边界

  sensor:
    publish_rate_hz: 10.0
    camera_frame: "camera_link"
    camera_fov_deg:
      horizontal: 70.0
      vertical: 55.0
    max_range_m: 3.0
    pose_noise_std:
      xyz: 0.0                                # 默认真值；需要鲁棒性测试时再加噪声
      yaw: 0.0
    output_mode: "adapter_input"              # adapter_input | adapter_bypass
    publish_truth_cloud: true
    truth_cloud_points_per_face: 64

  fake_base:
    odom_frame: "odom"
    base_frame: "2d_base_link"
    integration_rate_hz: 50.0
    coarse_goal_duration_sec: 1.0             # 粗导航完美到位的模拟耗时
    fine_align:
      initial_position_offset_m: 0.10
      initial_yaw_offset_rad: 0.0873          # 约 5 deg
      max_linear_x: 0.05
      max_angular_z: 0.20

  moveit_mock:
    enabled: false
    planning_group_left: "left_arm"
    planning_group_right: "right_arm"
    use_ros2_control_mock_components: true

  debug:
    publish_markers: true
    marker_topic: "/sim/markers"
    publish_private_topics: true              # /sim/* 只给 rosbag / RViz 调试，生产节点不订阅
```

关键约束：

```text
1. `output_mode=adapter_input`：sim_world 模拟上游 `/box_perception/result` 与 `/cloud_registered_body`，用于验证 perception_adapter 的 TF/重打包逻辑。
2. `output_mode=adapter_bypass`：当前环境缺 `box_perception_msgs` 时，sim_world 直接发布 `/perception/box_detections`，只用于策略层几何闭环。
3. fake_base 可以发布 odom/TF，但不得静态覆盖真实 `map→odom` 或 `camera_init→odom` 语义。
4. `/sim/*` Topic 只允许测试脚本、RViz、rosbag 订阅，生产节点禁止依赖。
```

---

## 6. launch 文件结构

### 6.1 完整真机启动

```python
# launch/bringup_full.launch.py
def generate_launch_description():
    config_dir = get_package_share_directory("fsm_config")
    business    = os.path.join(config_dir, "params", "business.yaml")
    fsm_params  = os.path.join(config_dir, "params", "fsm.yaml")
    interfaces  = os.path.join(config_dir, "params", "interfaces.yaml")
    errors      = os.path.join(config_dir, "params", "error_codes.yaml")
    logging     = os.path.join(config_dir, "params", "logging.yaml")

    common_params = [business, fsm_params, interfaces, errors, logging]

    return LaunchDescription([
        Node(package="safety_monitor", executable="safety_monitor_node",
             parameters=common_params),
        Node(package="task_manager", executable="task_manager_node",
             parameters=common_params),
        Node(package="wall_destacking_strategy", executable="wall_destacking_strategy_node",
             parameters=common_params),
        Node(package="perception_adapter", executable="perception_adapter_node",
             parameters=common_params),
        Node(package="navigation_manager", executable="navigation_manager_node",
             parameters=common_params),
        Node(package="vacuum_io", executable="vacuum_io_node",
             parameters=common_params),
        Node(package="pair_grasp_execution", executable="pair_grasp_execution_node",
             parameters=common_params),
        # perception_node / Nav2 / Fast-LIO / chassis_node / move_group 由外部 bringup 启动
    ])
```

### 6.2 用 mock 替代部分节点

```python
# launch/bringup_with_mock.launch.py
# 通过 LaunchArguments 选择是否启用 mock_nav / mock_grasp / mock_perception
```

### 6.3 只启策略层（开发期调试）

```python
# launch/bringup_strategy_only.launch.py
# 只启 strategy + 3 个 mock，方便单进程调试
```

### 6.4 仿真启动（M2-SIM / L3-SIM）

```python
# launch/bringup_with_kinematic_sim.launch.py
# 通过 LaunchArguments 选择 sim 输出模式和真实 adapter 组合：
# - sim_perception_output:=adapter_input|adapter_bypass
# - use_real_navigation_adapter:=true|false
# - use_real_grasp_adapter:=true|false
# - use_fake_nav2:=true|false
# - use_moveit_mock:=true|false
# L3-SIM-03 轻量物理 profile 建议另设 bringup_with_light_physics_sim.launch.py：
# - use_gazebo:=true|false
# - use_grasp_constraint_bridge:=false  # 阶段 B 才置 true
def generate_launch_description():
    config_dir = get_package_share_directory("fsm_config")
    business    = os.path.join(config_dir, "params", "business.yaml")
    fsm_params  = os.path.join(config_dir, "params", "fsm.yaml")
    interfaces  = os.path.join(config_dir, "params", "interfaces.yaml")
    errors      = os.path.join(config_dir, "params", "error_codes.yaml")
    logging     = os.path.join(config_dir, "params", "logging.yaml")
    sim_params  = os.path.join(config_dir, "params", "sim.yaml")

    common_params = [business, fsm_params, interfaces, errors, logging]
    sim_node_params = common_params + [sim_params]

    return LaunchDescription([
        # task_manager / strategy / safety_monitor 仍加载生产 common_params
        # sim_world_node / fake_nav2_base_node 加载 sim_node_params
        # MoveIt mock hardware 由 use_moveit_mock 控制
        # Gazebo world / ros2_control / MoveIt 由 L3-SIM-03 light physics launch 控制
    ])
```

详细见 `07_Mock规格与测试用例.md` 的 L3 / L3-SIM 分层。

---

## 7. 配置访问 Python 模板

每个节点用同一套读法：

```python
# wall_destacking_strategy/wall_destacking_strategy/node.py
from dataclasses import dataclass

@dataclass(frozen=True)
class BusinessConfig:
    box_size_l: float
    box_size_w: float
    box_size_h: float
    match_tol_y: float
    match_tol_z: float
    empty_confirm_frames: int
    max_retry_per_slot: int
    allow_single_arm_grasp: bool
    fixed_place_pose: tuple
    # ... 其他字段
    @classmethod
    def from_ros_params(cls, node: rclpy.node.Node) -> "BusinessConfig":
        return cls(
            box_size_l = node.declare_parameter("business.box_size.length", 0.40).value,
            box_size_w = node.declare_parameter("business.box_size.width",  0.40).value,
            box_size_h = node.declare_parameter("business.box_size.height", 0.40).value,
            match_tol_y = node.declare_parameter("business.match_tolerance.y", 0.15).value,
            match_tol_z = node.declare_parameter("business.match_tolerance.z", 0.15).value,
            empty_confirm_frames = node.declare_parameter("business.empty_confirm_frames", 5).value,
            max_retry_per_slot = node.declare_parameter("business.max_retry_per_slot", 3).value,
            allow_single_arm_grasp = node.declare_parameter("business.allow_single_arm_grasp", True).value,
            fixed_place_pose = (
                node.declare_parameter("business.fixed_place_pose.x", 0.5).value,
                node.declare_parameter("business.fixed_place_pose.y", 0.0).value,
                node.declare_parameter("business.fixed_place_pose.z", 0.8).value,
                # ... qx/qy/qz/qw
            ),
            # ...
        )
```

**强约束**：
- 每个节点必须在 `__init__` 中一次性把所有需要的参数读入 `ctx.config`
- 状态代码读 `ctx.config.xxx`，**绝不**在状态内 `self.get_parameter()`
- `BusinessConfig` 必须 `@dataclass(frozen=True)`，运行时若需 reload，由 `/reload_config` 触发完整重建

---

## 8. 参数版本与变更管理

```yaml
# 每个 YAML 文件顶部加版本号注释
# version: 1.0.0
# last_updated: 2026-05-23
# changelog:
#   1.0.0 初版
```

变更规则：
- **patch 升级**（1.0.x）：现场调值，不影响其他模块
- **minor 升级**（1.x.0）：新增字段（带默认值，向后兼容）
- **major 升级**（x.0.0）：字段重命名/删除，需同步改代码

`sim.yaml` 的版本独立于生产 YAML：允许随 L3-SIM 用例快速迭代，但不得要求生产节点新增参数或字段。若仿真需要生产节点读取新参数，必须先回到 `business.yaml` / `interfaces.yaml` 评审。L3-SIM-03 的 Gazebo world/model/physics、Foxglove layout 和抓取规则文档随 `fsm_test/l3_sim_03_assets/` 管理。

---

## 9. TODO 字段清单（现场标定时必填）

下列字段当前为占位，部署到真机时必须实测：

| 文件 | 路径 | 说明 |
|---|---|---|
| business.yaml | `observation_pose.*` | 观察位姿（map 系），现场标 |
| business.yaml | `left_phase_workpose.*` | 左作业位姿，现场标 |
| business.yaml | `right_phase_workpose.*` | 右作业位姿，现场标 |
| business.yaml | `desired_distance_to_wall` | 作业距离 |
| business.yaml | `fixed_place_pose.*` | 放置位（base_link 系），现场标 |
| business.yaml | `match_tolerance.y/z` | 视觉准确度上线后微调 |
| business.yaml | `empty_confirm_frames` | 视觉漏检率上线后微调 |
| business.yaml | `left_arm_workspace / right_arm_workspace` | 工程师 B 提供实际臂工作范围后回填 |
| business.yaml | `perception_adapter.*` | 视觉 / 雷达真实频率上线后微调 |
| business.yaml | `fine_alignment.*` | 作业位对墙误差闭环上线后微调 |
| business.yaml | `vacuum.*` | 真空硬件量程、阈值、建压曲线确认后回填 |
| sim.yaml | `sim.*` | 仅 M2-SIM / L3-SIM 使用，不是现场必填项；修改随 L3-SIM 用例 review |

## 9.1 部署前置检查项

FSM 真机启动前必须先确认：

| 项 | 检查方式 | 说明 |
|---|---|---|
| twist_mux align 通道 | `ros2 topic info /cmd_vel_align` 后能被 twist_mux 订阅 | `twist_mux.yaml` 必须新增 align，优先级 50 |
| TF 桥接 | `ros2 run tf2_ros tf2_echo body base_link` | 必须有 `body → base_link` 静态 TF |
| 感知 adapter | `ros2 topic echo /perception/health --once` | 不直接等待上游 perception health |
| 真空 mock | `ros2 topic echo /vacuum/pressure_raw --once` | 真实硬件未接入前必须启 mock |
| 底盘恢复服务 | `ros2 service list` 包含 `/nav/base_recovery` | `/clear_error` 第 1/3/4 阶段依赖 |

## 9.2 M2-SIM / L3-SIM 前置检查项

仿真启动前必须先确认：

| 项 | 检查方式 | 说明 |
|---|---|---|
| sim 参数 | `ros2 param get /sim_world_node sim.enabled` | 必须为 true，且只在仿真 launch 中出现 |
| TF 链 | `ros2 run tf2_ros tf2_echo map 2d_base_link` + `ros2 run tf2_ros tf2_echo body base_link` | 仿真 profile 中导航链 `map→odom→2d_base_link` 与感知桥接 `body→base_link` 均可解析 |
| FINE_ALIGN 通道 | `ros2 topic echo /cmd_vel_align --once` | L3-SIM-02 中必须能看到真实 navigation_manager 发布微调速度 |
| 真值感知 | `ros2 topic hz /perception/box_detections` 或 `/box_perception/result` | 根据 `sim.sensor.output_mode` 选择检查对象 |
| MoveIt / 控制器 | `ros2 node list` 包含 `move_group`，`ros2 control list_controllers` 能看到仿真 controller | L3-SIM-03 第一版必需；几何 profile 可用 mock controller |
| Gazebo world | `gz topic -l` 或 Gazebo GUI / server 日志能看到 world、robot、container、box | 只对 L3-SIM-03 轻量物理 profile 必需 |
| ros2_control 仿真链 | `ros2 control list_controllers` 包含 `joint_state_broadcaster`、`dual_v5_arm_controller`、`torso_controller` | L3-SIM-03 第一版必须通过 |
| 抓取桥接器 | `ros2 node list` 包含仿真抓取桥接节点，且接触/约束调试 topic 可见 | 阶段 B 才必需，第一版不阻塞 |

---

## 10. 版本

- v1.0 初版 2026-05-23
- v1.1 2026-05-27：新增 M2-SIM 专用 `sim.yaml`、`bringup_with_kinematic_sim.launch.py` 配置说明和仿真前置检查项
- v1.2 2026-06-11：L3-SIM-03 改为轻量物理仿真第一版；Gazebo 资产与抓取桥接器保持测试专用，不进入生产 YAML
